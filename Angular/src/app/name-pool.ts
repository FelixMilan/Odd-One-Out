//temporary
export let namePool: string[] = [
    'Bob Marley',
    'Barack Obama',
    'Jigglypuff',
    'Taylor Swift'
];
 /*
function getRandomInt(min: number, max: number): number {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1) + min);
}
*/